import Enum from './enum.js';

export default class Message extends Enum {
  static EVERTHING = 'Click New Event to create your first point';
  static PAST = 'There are no past events now';
  static FUTURE = 'There are no future events now';
}
